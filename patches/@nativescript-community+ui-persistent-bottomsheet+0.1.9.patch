diff --git a/node_modules/@nativescript-community/ui-persistent-bottomsheet/index.js b/node_modules/@nativescript-community/ui-persistent-bottomsheet/index.js
index a9ea10a..1a2bbec 100644
--- a/node_modules/@nativescript-community/ui-persistent-bottomsheet/index.js
+++ b/node_modules/@nativescript-community/ui-persistent-bottomsheet/index.js
@@ -54,6 +54,7 @@ let PersistentBottomSheet = class PersistentBottomSheet extends AbsoluteLayout {
         this.isAnimating = false;
         this.prevDeltaY = 0;
         this.viewHeight = 0;
+        this.viewWidth = 0;
         this.wasDraggingPanel = false;
         this.gestureModeDecided = false;
         this._translationY = -1;
@@ -119,6 +120,16 @@ let PersistentBottomSheet = class PersistentBottomSheet extends AbsoluteLayout {
                 return false;
             }
         }
+        // Check if gesture is within bottomSheet horizontal bounds (for partial-width panels in landscape)
+        if (this.bottomSheet) {
+            const bsWidth = Utils.layout.toDeviceIndependentPixels(this.bottomSheet.getMeasuredWidth());
+            if (bsWidth > 0 && bsWidth < this.viewWidth) {
+                const loc = this.bottomSheet.getLocationRelativeTo(this);
+                if (loc && (data.x < loc.x || data.x > loc.x + bsWidth)) {
+                    return false;
+                }
+            }
+        }
         return true;
     }
     get translationY() {
@@ -317,7 +328,9 @@ let PersistentBottomSheet = class PersistentBottomSheet extends AbsoluteLayout {
     onLayoutChanged(event) {
         const contentView = event.object;
         const height = Math.round(Utils.layout.toDeviceIndependentPixels(contentView.getMeasuredHeight()));
+        const width = Math.round(Utils.layout.toDeviceIndependentPixels(contentView.getMeasuredWidth()));
         this.viewHeight = height;
+        this.viewWidth = width;
         if (this.bottomSheet) {
             this.bottomSheet.top = {
                 unit: 'px',
